services:
  app:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ragops-state:/app/.state
      - ragops-identities:/app/.secrets
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
volumes:
  ragops-state:
  ragops-identities:
